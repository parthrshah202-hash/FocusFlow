// Member B — Timer component stub
// Replace this with the real implementation from Member B

const Timer = () => (
  <div className="flex items-center justify-center h-64 text-slate-500 text-sm border border-dashed border-white/10 rounded-2xl">
    ⏳ Timer component (Member B)
  </div>
);

export default Timer;
